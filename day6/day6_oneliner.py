from pathlib import Path; print('\n'.join([["The sum over the groups of questions from which any was answered with 'yes' is: {0}".format(sum((len(set(line.replace('\n', ''))) for line in groups))),"The sum of questions to which yes was answered by all group members summed over all groups: {0}".format(sum(sum(1 for letter in set(''.join(group)) if ''.join(group).count(letter) == len(group))for group in [line.split('\n') for line in groups]))] for groups in [Path(Path(__file__).parent / 'input_day6.txt').resolve().read_text().split('\n\n')]][0]))
